//
class Selection {
  constructor({ type, eliteCount, tournamentSize }) {
    this.type = type;
    this.eliteCount = eliteCount;
    this.tournamentSize = tournamentSize;
  }
}

export default Selection;
